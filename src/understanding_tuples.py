"""
las tuples so listas de elementos que no pueden 
cambiar su tamaño se utilizan los paretecis para definir un a tupla

"""
rectagle_dimension=(200,50) # esto de aqui es una

print(f"largo, {rectagle_dimension[0]}mm")
print(f"ancho, {rectagle_dimension[1]}mm")



"""
no podemos modificar una tupla, ni tampoco agregar elementos 
los que si podemos hacer es cambiar la asignacion 
a una variable que almacena una tupla


"""
rectagle_dimension= (300,100)
# aqui solo se cambia el valor de la tupla, bueno no esta cambiando 
# sino que esta remplazando los valores anteriores